# Changelog

All notable changes to the CCL test data will be documented in this file.

{% for version, release in ctx.history.released.items() %}
## [{{ version.as_tag() | replace("v", "", 1) }}] - {{ release.tagged_date.strftime("%Y-%m-%d") }}
{% for type_, commits in release["elements"] | dictsort %}
{% if type_ != "unknown" and commits | length > 0 %}

### {{ type_ | title }}
{% for commit in commits %}
- {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{{ commit.descriptions[0] | capitalize }} ([`{{ commit.hexsha[:7] }}`]({{ commit.hexsha | commit_hash_url }}))
{%- if commit.descriptions | length > 1 %}

  {{ commit.descriptions[1:] | join("\n\n  ") | indent(2, first=false) }}
{%- endif %}
{% endfor %}
{% endif %}
{% endfor %}
{% endfor %}
