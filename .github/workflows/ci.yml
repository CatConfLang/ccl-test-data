name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install jv validator
        run: |
          curl -sL https://github.com/santhosh-tekuri/jsonschema/releases/download/cmd%2Fjv%2Fv0.7.0/jv-v0.7.0-linux-amd64.tar.gz | tar xz
          sudo mv jv /usr/local/bin/

      - name: Configure go.mod for CI
        run: |
          go mod edit -dropreplace=github.com/tylerbu/ccl-test-lib
          go mod tidy

      - name: Install Node dependencies
        working-directory: scripts
        run: npm ci

      - name: Validate JSON schemas
        run: |
          jv schemas/source-format.json source_tests/**/*.json
          jv schemas/generated-format.json generated_tests/*.json

      - name: Lint
        run: |
          go fmt ./...
          go vet ./...

      - name: Generate and test
        run: |
          go run ./cmd/ccl-test-runner generate-flat --source ./source_tests/core
          go run ./cmd/ccl-test-runner generate --run-only function:parse \
            --skip-tags behavior:crlf_preserve_literal,behavior:tabs_preserve,behavior:tabs_to_spaces,behavior:strict_spacing
          go run ./cmd/ccl-test-runner test --basic-only

      - name: Check docs are up-to-date
        run: |
          cd scripts && node update-readme-remark.mjs
          git diff --exit-code README.md
