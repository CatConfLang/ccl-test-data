[tools]
go = "1.25.4"
hk = "latest"
just = "latest"
# Node.js for running ESM scripts and commitlint
node = "latest"
# Python package manager for semantic-release
uv = "latest"

[env]
# Ensure tools are available in PATH
_.path = [
    "{{config_root}}/bin",
    "$PATH"
]
